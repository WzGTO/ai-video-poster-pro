# 🎬 คู่มือติดตั้ง AI Video Poster Pro - ฉบับภาษาไทย

## 📋 สารบัญ
1. [ข้อกำหนดเบื้องต้น](#ข้อกำหนดเบื้องต้น)
2. [ติดตั้ง Node.js และ npm](#ติดตั้ง-nodejs-และ-npm)
3. [ดาวน์โหลดโปรเจค](#ดาวน์โหลดโปรเจค)
4. [ติดตั้ง Dependencies](#ติดตั้ง-dependencies)
5. [ตั้งค่า Google OAuth และ Drive API](#ตั้งค่า-google-oauth-และ-drive-api)
6. [ตั้งค่า Environment Variables](#ตั้งค่า-environment-variables)
7. [เริ่มใช้งาน](#เริ่มใช้งาน)
8. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## ข้อกำหนดเบื้องต้น

ก่อนเริ่มติดตั้ง คุณต้องมี:

- ✅ **คอมพิวเตอร์** Windows, Mac หรือ Linux
- ✅ **บัญชี Google** สำหรับ login และเก็บข้อมูล
- ✅ **อินเทอร์เน็ต** ความเร็วพอสมควร
- ✅ **พื้นที่ว่าง** อย่างน้อย 500 MB

---

## ติดตั้ง Node.js และ npm

### Windows

1. **ดาวน์โหลด Node.js**
   - เปิดเว็บ https://nodejs.org/
   - คลิกปุ่ม "Download Node.js (LTS)" สีเขียวใหญ่
   - เลือกเวอร์ชัน **20.x LTS** หรือสูงกว่า

2. **ติดตั้ง**
   - เปิดไฟล์ที่ดาวน์โหลด (เช่น `node-v20.10.0-x64.msi`)
   - คลิก "Next" ตลอด
   - ✅ **สำคัญ**: ติ๊กช่อง "Automatically install necessary tools"
   - คลิก "Install"
   - รอจนเสร็จ (ประมาณ 5-10 นาที)

3. **ตรวจสอบการติดตั้ง**
   - เปิด **Command Prompt** (กด Windows+R แล้วพิมพ์ `cmd`)
   - พิมพ์คำสั่ง:
     ```bash
     node --version
     ```
   - ต้องเห็นเลขเวอร์ชัน เช่น `v20.10.0`
   - พิมพ์คำสั่ง:
     ```bash
     npm --version
     ```
   - ต้องเห็นเลขเวอร์ชัน เช่น `10.2.0`

### Mac

1. **ติดตั้งผ่าน Homebrew** (แนะนำ)
   ```bash
   # ติดตั้ง Homebrew ก่อน (ถ้ายังไม่มี)
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   
   # ติดตั้ง Node.js
   brew install node@20
   ```

2. **หรือ ดาวน์โหลดไฟล์ติดตั้ง**
   - เข้า https://nodejs.org/
   - ดาวน์โหลดไฟล์ `.pkg` สำหรับ Mac
   - เปิดไฟล์และติดตั้งตามขั้นตอน

3. **ตรวจสอบ**
   - เปิด **Terminal**
   - พิมพ์ `node --version` และ `npm --version`

---

## ดาวน์โหลดโปรเจค

### วิธีที่ 1: ใช้ Git (แนะนำ)

1. **ติดตั้ง Git** (ถ้ายังไม่มี)
   - Windows: ดาวน์โหลดจาก https://git-scm.com/
   - Mac: `brew install git`

2. **Clone โปรเจค**
   ```bash
   # เปิด Command Prompt หรือ Terminal
   cd Desktop
   git clone <repository-url>
   cd AutoPost
   ```

### วิธีที่ 2: ดาวน์โหลดไฟล์ ZIP

1. ดาวน์โหลดไฟล์ ZIP ของโปรเจค
2. แตกไฟล์ไปที่ Desktop หรือที่ที่ต้องการ
3. เปิด Command Prompt/Terminal ไปที่โฟลเดอร์นั้น
   ```bash
   cd Desktop/AutoPost
   ```

---

## ติดตั้ง Dependencies

1. **ติดตั้ง packages ที่จำเป็นทั้งหมด**
   ```bash
   npm install
   ```
   
   > ⏱️ **รอ 5-10 นาที** (ขึ้นกับความเร็วอินเทอร์เน็ต)
   > 
   > จะดาวน์โหลด packages ประมาณ 300-500 MB

2. **ตรวจสอบว่าติดตั้งสำเร็จ**
   ```bash
   npm list next
   ```
   
   ต้องเห็น `next@15.x.x`

---

## ตั้งค่า Google OAuth และ Drive API

### ขั้นตอนที่ 1: สร้าง Google Cloud Project

1. **เข้า Google Cloud Console**
   - เปิด https://console.cloud.google.com/
   - Login ด้วยบัญชี Google ของคุณ

2. **สร้างโปรเจคใหม่**
   - คลิกที่ชื่อโปรเจคด้านบน (หรือ "Select a project")
   - คลิก "NEW PROJECT"
   - ตั้งชื่อ: `AI Video Poster Pro`
   - คลิก "CREATE"
   - รอสักครู่ แล้วเลือกโปรเจคที่สร้างใหม่

### ขั้นตอนที่ 2: เปิดใช้งาน APIs

1. **ไปที่ API Library**
   - เมนูด้านซ้าย → "APIs & Services" → "Library"

2. **เปิดใช้ APIs ต่อไปนี้**
   
   **a) Google Drive API** (สำคัญมาก! ✨)
   - ค้นหา "Google Drive API"
   - คลิก
   - คลิก "ENABLE"
   
   **b) Google+ API**
   - ค้นหา "Google+ API"
   - คลิก "ENABLE"
   
   **c) YouTube Data API v3** (ถ้าจะใช้ YouTube)
   - ค้นหา "YouTube Data API v3"
   - คลิก "ENABLE"

### ขั้นตอนที่ 3: สร้าง OAuth Credentials

1. **ไปที่ Credentials**
   - เมนูด้านซ้าย → "APIs & Services" → "Credentials"

2. **ตั้งค่า OAuth Consent Screen ก่อน**
   - คลิกแท็บ "OAuth consent screen"
   - เลือก **"External"** (เพื่อให้ใครๆ ก็ Login ได้)
   - คลิก "CREATE"
   
   **กรอกข้อมูล:**
   - App name: `AI Video Poster Pro`
   - User support email: อีเมลของคุณ
   - Developer contact: อีเมลของคุณ
   - คลิก "SAVE AND CONTINUE"
   
   **Scopes:**
   - คลิก "ADD OR REMOVE SCOPES"
   - เลือก:
     - ✅ `.../auth/userinfo.email`
     - ✅ `.../auth/userinfo.profile`
     - ✅ `openid`
     - ✅ `.../auth/drive.file` (สำคัญ! 🔥)
     - ✅ `.../auth/youtube.upload` (ถ้าใช้ YouTube)
   - คลิก "UPDATE"
   - คลิก "SAVE AND CONTINUE"
   
   **Test users:**
   - คลิก "ADD USERS"
   - ใส่อีเมล Google ของคุณ
   - คลิก "ADD"
   - คลิก "SAVE AND CONTINUE"

3. **สร้าง OAuth Client ID**
   - กลับไปแท็บ "Credentials"
   - คลิก "CREATE CREDENTIALS" → "OAuth client ID"
   - Application type: **"Web application"**
   - Name: `AI Video Poster Web Client`
   
   **Authorized JavaScript origins:**
   - คลิก "ADD URI"
   - ใส่: `http://localhost:3000`
   
   **Authorized redirect URIs:**
   - คลิก "ADD URI"
   - ใส่: `http://localhost:3000/api/auth/callback/google`
   
   - คลิก "CREATE"

4. **บันทึก Client ID และ Secret**
   - จะมีหน้าต่าง popup แสดง:
     - ✅ **Client ID** (ยาวๆ ลงท้าย `.apps.googleusercontent.com`)
     - ✅ **Client secret** (อักขระสุ่ม)
   - **คัดลอกเก็บไว้** (จะใช้ในขั้นตอนถัดไป)
   - หรือคลิก "DOWNLOAD JSON" เก็บไว้

---

## ตั้งค่า Environment Variables

1. **สร้างไฟล์ `.env.local`**
   ```bash
   # คัดลอกไฟล์ตัวอย่าง
   copy .env.local.example .env.local
   ```
   
   > Mac/Linux ใช้: `cp .env.local.example .env.local`

2. **แก้ไขไฟล์ `.env.local`**
   - เปิดไฟล์ด้วย Notepad หรือ VS Code
   - หาบรรทัดเหล่านี้และกรอกค่า:

   ```bash
   # ===== NextAuth Configuration =====
   NEXTAUTH_SECRET=your-secret-key-here
   NEXTAUTH_URL=http://localhost:3000

   # ===== Google OAuth =====
   GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
   GOOGLE_CLIENT_SECRET=xxxxx-xxxxx

   # ===== AI Models =====
   GEMINI_API_KEY=xxxxx
   ```

3. **สร้าง NEXTAUTH_SECRET**
   
   **Windows (PowerShell):**
   ```powershell
   # เปิด PowerShell
   -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
   ```
   
   **Mac/Linux:**
   ```bash
   openssl rand -base64 32
   ```
   
   คัดลอกผลลัพธ์ที่ได้ไปใส่แทน `your-secret-key-here`

4. **ดาวน์โหลด Gemini API Key** (ฟรี!)
   - เข้า https://ai.google.dev/
   - คลิก "Get API key in Google AI Studio"
   - Login ด้วย Google
   - คลิก "Create API key"
   - คัดลอก API key
   - นำไปใส่ที่ `GEMINI_API_KEY`

5. **บันทึกไฟล์**

---

## เริ่มใช้งาน

1. **รัน Development Server**
   ```bash
   npm run dev
   ```

2. **รอจนเห็นข้อความ**
   ```
   ✓ Ready in 2.5s
   ○ Local: http://localhost:3000
   ```

3. **เปิดเว็บเบราว์เซอร์**
   - เปิด Google Chrome หรือเบราว์เซอร์ที่ชอบ
   - ไปที่: **http://localhost:3000**

4. **คลิก "เข้าสู่ระบบด้วย Google"**
   - เลือกบัญชี Google
   - อนุญาตให้แอปเข้าถึง Google Drive ✅
   - จะถูกพาไปหน้า Dashboard

5. **เริ่มใช้งาน! 🎉**

---

## โครงสร้างข้อมูลบน Google Drive

เมื่อคุณใช้งาน แอปจะสร้างโฟลเดอร์นี้บน Google Drive ของคุณ:

```
📁 Google Drive ของฉัน/
└── 📁 AI Video Poster Pro/          ← แอปสร้างให้อัตโนมัติ
    ├── 📄 products.json             ← รายการสินค้า
    ├── 📄 settings.json             ← การตั้งค่าของคุณ
    ├── 📄 templates.json            ← template ที่บันทึกไว้
    └── 📁 videos/                   ← วิดีโอที่สร้าง
        ├── 📁 video-xxx/
        │   ├── 🎬 video.mp4
        │   ├── 🖼️ thumbnail.jpg
        │   └── 📄 metadata.json
        └── 📁 video-yyy/
            └── ...
```

> 💡 **ข้อดี**: 
> - ข้อมูลทั้งหมดอยู่บน Drive ของคุณ ไม่ได้เก็บบนเซิร์ฟเวอร์
> - ลบแอปได้เมื่อไหร่ก็ได้ ข้อมูลยังอยู่
> - เปลี่ยนอุปกรณ์ก็ยังใช้งานต่อได้ (login ด้วย Google เดิม)

---

## การแก้ไขปัญหา

### ❌ ปัญหา: `npm: command not found`

**สาเหตุ**: ยังไม่ได้ติดตั้ง Node.js

**แก้ไข**:
1. ติดตั้ง Node.js ตาม[คู่มือด้านบน](#ติดตั้ง-nodejs-และ-npm)
2. ปิด Command Prompt แล้วเปิดใหม่
3. ลองพิมพ์ `node --version` อีกครั้ง

---

### ❌ ปัญหา: OAuth Error - redirect_uri_mismatch

**สาเหตุ**: URL ใน Google Cloud Console ไม่ตรงกับ URL ที่แอปใช้

**แก้ไข**:
1. เช็คว่าใน Google Cloud Console ตั้งค่า Redirect URI เป็น:
   ```
   http://localhost:3000/api/auth/callback/google
   ```
   (ต้องตรงทุกตัวอักษร ไม่มี trailing slash)

2. เช็คว่าไฟล์ `.env.local` มี:
   ```
   NEXTAUTH_URL=http://localhost:3000
   ```

3. Restart dev server (`Ctrl+C` แล้ว `npm run dev` ใหม่)

---

### ❌ ปัญหา: "Access blocked: This app's request is invalid"

**สาเหตุ**: OAuth Consent Screen ยังไม่ได้ตั้งค่าครบ

**แก้ไข**:
1. ไป Google Cloud Console
2. APIs & Services → OAuth consent screen
3. เช็คว่าเพิ่ม **Test users** แล้ว (ใส่อีเมล Google ของคุณ)
4. เช็คว่าเพิ่ม **Scopes** แล้ว (ต้องมี drive.file)

---

### ❌ ปัญหา: "Failed to read products from Google Drive"

**สาเหตุ**: ไม่มีสิทธิ์เข้าถึง Google Drive

**แก้ไข**:
1. เช็คว่าเปิดใช้งาน **Google Drive API** แล้วใน Google Cloud Console
2. เช็คว่า OAuth Scopes มี `https://www.googleapis.com/auth/drive.file`
3. ลอง **Logout แล้ว Login ใหม่** (เพื่อขอ permission ใหม่)
4. ตอน Login ต้อง**อนุญาตให้เข้าถึง Google Drive** ✅

---

### ❌ ปัญหา: Port 3000 already in use

**สาเหตุ**: มีโปรแกรมอื่นใช้พอร์ต 3000 อยู่

**แก้ไข**:
```bash
# ใช้พอร์ตอื่นแทน
PORT=3001 npm run dev
```

แล้วเปิด http://localhost:3001

---

### ❌ ปัญหา: แอปช้า หรือโหลดนาน

**แก้ไข**:
1. เช็คว่ามีอินเทอร์เน็ต
2. ลอง Refresh หน้า (F5)
3. เคลียร์ cache เบราว์เซอร์ (Ctrl+Shift+Delete)
4. ลอง Restart dev server

---

## 📞 ขอความช่วยเหลือ

ถ้ายังแก้ไขไม่ได้:

1. เช็คว่าทำตาม[ขั้นตอนทั้งหมด](#สารบัญ)แล้ว
2. ตรวจสอบ console ใน Command Prompt/Terminal มี error อะไรบ้าง
3. ลอง Google หา error message
4. ถาม ChatGPT หรือ Gemini พร้อมแนบ error message

---

## 🎯 สรุปขั้นตอนย่อ

1. ติดตั้ง Node.js (v20+)
2. ดาวน์โหลดโปรเจค
3. `npm install`
4. สร้าง Google Cloud Project + OAuth
5. เปิดใช้ Google Drive API
6. ตั้งค่า `.env.local`
7. `npm run dev`
8. เปิด http://localhost:3000
9. Login ด้วย Google
10. เริ่มใช้งาน! 🚀

---

**สำเร็จแล้ว! 🎊** คุณพร้อมสร้างวิดีโอโฆษณาด้วย AI แล้ว!

**หมายเหตุ**: ข้อมูลทั้งหมดจะเก็บบน **Google Drive ของคุณ** ไม่มีการเก็บข้อมูลถาวรบนเซิร์ฟเวอร์ ปลอดภัย 100% 🔒
